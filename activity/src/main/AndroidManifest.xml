<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.ycdage.activity">

    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name="com.ycdage.activity.simple.NotificationActivity"/>
        <activity android:name="com.ycdage.activity.screen.ScreenUtilActivity"/>
        <activity
            android:name="com.ycdage.activity.fingerkey.FignerActivity"
            android:label="@string/title_activity_figner"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".simple.ImplicitIntentActivity"/>
        <activity android:name="com.ycdage.activity.recyclerview.RecyclerViewActivity"/>
        <activity android:name=".simple.HandlerUseActivity"/>
        <activity android:name=".simple.AsyncTaskActivity"/>
        <activity android:name=".recyclerview.IMRecycleActivity"/>
        <activity android:name=".otheruse.OtherUserActivity"/>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.ycdage.firstlineofcode.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"
                tools:replace="android:resource"/>
        </provider>
        <service android:name=".simple.MyIntentService" />
    </application>

</manifest>